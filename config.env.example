# Copied to ~/Library/Application Support/InterviewTracker/config.env by install.sh.
# Keep credentials and private notification topics out of this repository.

# Set this only when `claude` is not available on launchd's PATH.
CLAUDE_BIN=claude

# Optional: a private ntfy topic for iPhone and Apple Watch notifications.
NTFY_TOPIC=
